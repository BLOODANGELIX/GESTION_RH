<UserControl x:Class="ejemplo.Views.PuestosView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:ejemplo.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <DataGrid Style="{StaticResource EstiloTablaRefinado}" AutoGenerateColumns="False" Margin="0,10,0,0">
        <DataGrid.Columns>
            <DataGridTextColumn Header="ID Puesto" Binding="{Binding IdPuesto}" Width="*"/>
            <DataGridTextColumn Header="Nombre" Binding="{Binding Nombre}" Width="*"/>
            <DataGridTextColumn Header="Salario" Binding="{Binding Salario}" Width="*"/>
        </DataGrid.Columns>
    </DataGrid>


</UserControl>
